from setuptools import setup, find_packages

setup(
      name='update',
      version='',
      packages=find_packages(),
      python_requires='>=3.0.*',
      install_requires=['inquirer==2.6.3']
)